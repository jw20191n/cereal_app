<h1><%=  @group.name  %></h1>
<h2><%= @group.location  %></h2>
<p>Members:</p>
<% @group.users.each do |user| %>
    <li>  <%= link_to user.name, user_path(user) %> </li>
<% end %>



<%= button_to "join group", { action:"join", :group_id => @group.id}, :method => :post %>